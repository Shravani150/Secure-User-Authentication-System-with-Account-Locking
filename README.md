# Secure-User-Authentication-System-with-Account-Locking
Java-based Secure User Authentication System with two implementations: a print-based validation version and an exception-driven design. It enforces strong passwords, handles login attempts, locks accounts after failures, and demonstrates the evolution from basic control flow to robust exception handling using OOP principles.
